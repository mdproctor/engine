# io.casehub.api.plan.PlanElement

**Package:** `io.casehub.api.plan`

**Kind:** `interface`

Marker interface for elements that can be wrapped in a `PlanItem` and tracked through a
CMMN lifecycle.

<p>Permitted implementations: `io.casehub.api.model.Worker`, `io.casehub.engine.planning.stage.Stage`, `io.casehub.engine.planning.stage.SubCase`.

<p>Not sealed — implementations span Maven module boundaries.
