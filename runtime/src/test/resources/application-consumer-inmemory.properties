# Test profile mimicking consumer (e.g. AML) CDI config:
# - In-memory repos for EventLog, CaseMetaModel via selected-alternatives
# - CaseInstance repos via @Priority(1) auto-activation (NOT selected-alternatives)
# Reproduces engine#663 regression reported from AML after #680 workaround removal.

quarkus.quartz.store-type=ram

quarkus.index-dependency.scheduler-quartz.group-id=io.casehub
quarkus.index-dependency.scheduler-quartz.artifact-id=casehub-engine-scheduler-quartz
quarkus.index-dependency.persistence-memory.group-id=io.casehub
quarkus.index-dependency.persistence-memory.artifact-id=casehub-engine-persistence-memory
quarkus.index-dependency.cbr-inmem.group-id=io.casehub
quarkus.index-dependency.cbr-inmem.artifact-id=casehub-neocortex-memory-cbr-inmem

# Deliberately omit InMemoryCaseInstanceRepository and InMemoryReactiveCaseInstanceRepository
# from selected-alternatives — they activate via @Priority(1) subclasses instead.
quarkus.arc.selected-alternatives=\
  io.casehub.persistence.memory.InMemoryCaseMetaModelRepository,\
  io.casehub.persistence.memory.InMemoryReactiveCaseMetaModelRepository,\
  io.casehub.persistence.memory.InMemoryEventLogRepository,\
  io.casehub.persistence.memory.InMemoryReactiveEventLogRepository,\
  io.casehub.neocortex.memory.cbr.inmem.InMemoryCbrCaseMemoryStore

quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:consumerinmemtest;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
quarkus.datasource.username=sa
quarkus.datasource.password=
quarkus.hibernate-orm.schema-management.strategy=drop-and-create
quarkus.flyway.migrate-at-start=false
