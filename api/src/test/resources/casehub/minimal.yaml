dsl: "0.1"
version: "1.0.0"
name: Minimal
namespace: test
spec:
  capabilities:
    - name: process
      inputProjection: "."
      outputProjection: "."
  bindings:
    - name: trigger
      capability: process
      on:
        contextChange:
          filter: '.ready == true'
