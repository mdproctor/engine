namespace: test
name: unlimited-case
version: "1.0"
spec:
  capabilities:
    - name: doWork
      inputProjection: "."
  workers:
    - name: worker-1
      capabilities: [doWork]
  bindings:
    - name: do-work-binding
      capability: doWork
      on:
        contextChange:
          filter: '.ready == true'
